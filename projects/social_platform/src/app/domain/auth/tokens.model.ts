/** @format */

/**
 * Модель токенов аутентификации
 *
 * Назначение: Определяет структуру JWT токенов для аутентификации
 * Принимает: Не принимает параметров (класс модели)
 * Возвращает: Типизированный объект с access и refresh токенами
 *
 * Функциональность:
 * - Содержит access токен для авторизации запросов
 * - Содержит refresh токен для обновления access токена
 * - Используется для хранения и передачи токенов аутентификации
 */
export class Tokens {
  access!: string;
  refresh!: string;
}
